#include <stdio.h>

struct edge {
    int u, v, w;
};

int main () {

    int number_of_vertices;
    scanf("%d", &number_of_vertices);
    
    struct edge* graph = (struct edge *)malloc(sizeof(struct edge));
    return 0;
}