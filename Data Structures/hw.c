#include <stdio.h>
#include <stdlib.h>

struct customers {
    char code;
    int count;
};

int main () {
    int front, size = 0;
    int rear = -1;
    struct customers* array;

    int input;
    while (scanf("%d", &input)) {
        switch (input) {
            case -1 :
                break;
            case 1:
                struct customers customer;
                
                scanf(" %c", &customer->code)
        } 
    }


}

